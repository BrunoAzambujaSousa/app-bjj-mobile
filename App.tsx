// App.tsx

import React from 'react';
import LoginScreen from './src/screens/LoginScreen'; // Importando nossa nova tela

export default function App() {
  // Agora, em vez de mostrar o texto padrão, ele vai mostrar nossa tela de login.
  return <LoginScreen />;
}